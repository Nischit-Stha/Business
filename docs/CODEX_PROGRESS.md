# Codex progress

- **Current task:** Veera V2 agreements and weekly payment scheduling.
- **Status:** PASS — implementation and all requested local verification completed. No commit created.
- **Files created:** `supabase/migrations/20260824030000_agreements_weekly_payments.sql`; `supabase/tests/agreements_payments_test.sql`; `apps/web/src/lib/agreements.ts`; `apps/web/src/lib/agreement-actions.ts`; `apps/web/src/components/agreement-form.tsx`; Agreements list/new/detail pages under `apps/web/src/app/agreements/`; `apps/web/src/app/payments/page.tsx`; `docs/payment-allocation.md`.
- **Files modified:** `apps/web/src/components/staff-nav.tsx`; `apps/web/src/app/styles.css`; this progress file.
- **Database changes:** Added `agreements`, `payment_schedule_items`, immutable `payment_transactions`, and immutable `payment_allocations`; agreement/RTO constraints and indexes; active-vehicle agreement uniqueness; active-assignment protection; deny-by-default RLS and read-only active-staff policies; controlled create, lifecycle, schedule generation, manual receipt, and reversal RPCs; overdue/payment summary and RTO progress views; configurable owner exception RPC; expanded audit event actions.
- **Features implemented:** Draft/pending/active/suspended/completed/cancelled agreement lifecycle; weekly rental, rent-to-own, and short-term types; first-due-date schedule generation; end-date/payment-count/total-amount limits; safe 26-week open-ended horizon; idempotent schedule extension; manual verified PayID receipt entry; deterministic FIFO allocation across exact, partial, multi-week, and advance payments; explicit unallocated balances; compensating reversals; current overdue and next-payment reporting; rent-to-own payment progress; owner exception groundwork; operational Agreements and Payments staff pages.
- **Tests/results:** `npm run lint` PASS; `npm run typecheck` PASS; `npm test` PASS (6 tests); `npm run build` PASS; `npm run supabase:reset` PASS; `npm exec supabase -- test db` PASS (56 tests); `npm exec supabase -- db lint --local` PASS with no schema errors; `git diff --check` PASS. New database coverage includes creation, controlled activation, active-assignment requirement, vehicle conflict prevention, weekly/end-date schedule generation, idempotence, overdue totals, exact/partial/multi-week/advance/excess payments, explicit unallocated funds, paid-obligation reporting, immutable transactions, reversal history/audit, unauthorized denial, and inactive-staff denial.
- **Unresolved issues:** No automatic bank/PayID reconciliation, SMS, Renta, Starr365, toll/fine, maintenance, contract PDF, ownership transfer, credit/legal, notification, production migration, or V1 data migration was implemented, as required. Schedule extension is currently a manual controlled staff action; a future scheduled server job can call the same idempotent function. Reversal preserves original allocations and deliberately does not retroactively reallocate later receipts; resulting exceptions require staff review. Agreement activation assumes the vehicle assignment has already been created. Vehicle swaps with an active agreement require the agreement to be suspended/closed first until a separately designed atomic agreement-aware swap workflow exists.
- **Security notes:** Active staff authorization is enforced inside every mutation function and by RLS. Authenticated browser roles receive SELECT only and cannot directly write agreements or financial tables. Agreement lifecycle mutations are RPC-controlled. Receipt and allocation history rejects update/delete; corrections are negative compensating records. Closing a vehicle assignment while its matching agreement is active is blocked. Audit metadata contains operational IDs and amounts only, with no bank details. No service-role key, production credential, real PII, or private document was added.
- **Git status:** Task changes remain uncommitted. Modified: `apps/web/src/app/styles.css`, `apps/web/src/components/staff-nav.tsx`, `docs/CODEX_PROGRESS.md`. New/untracked: agreements pages, payments page, agreement component/server modules, payment allocation documentation, agreements/payments migration, and pgTAP test. No previous migration, V1 file, seed data, or production system was changed.
- **Recommended next task:** Review the agreement activation/payment/reversal workflow in the local UI, then design an agreement-aware atomic vehicle swap and a scheduled server job that extends open-ended schedules and surfaces owner exceptions. Keep external integrations and production migration as separately reviewed tasks.
