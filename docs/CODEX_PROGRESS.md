# Codex progress

- **Current task:** Veera V2 toll/fine matching, communications history, and automated payment-reminder groundwork.
- **Status:** PASS — implementation complete and every requested local verification passed. No commit, production migration, provider integration, message delivery, Starr365 write-back, production credential, or real customer data was created.
- **Files created:** `supabase/migrations/20260824060000_tolls_fines_collections.sql`; `supabase/tests/tolls_collections_test.sql`; `apps/web/src/lib/collections-actions.ts`; `apps/web/src/app/tolls-fines/page.tsx`; `apps/web/src/app/tolls-fines/[id]/page.tsx`; `apps/web/src/app/collections/page.tsx`.
- **Files modified:** `apps/web/src/components/staff-nav.tsx`; `apps/web/src/app/customers/[id]/page.tsx`; `docs/CODEX_PROGRESS.md`.
- **Database changes:** Added durable `toll_fine_notices`, append-only `notice_match_results`, reviewed `notice_allocations`, `notice_status_history`, minimal `communications`, `payment_promises`, configurable `reminder_rules`, and queued `reminder_actions`. Added read-only active-staff RLS, indexes/partial uniqueness, validation constraints, audit actions, collection/toll exception types, and security-definer RPC workflows for notice creation/matching, driver review, lifecycle transitions, contact logging, promises, and reminder generation.
- **Features implemented:** Deterministic assignment-time matching using vehicle and half-open custody ranges; explicit no-match/ambiguous review state; retained candidate evidence and original match result; reason-required manual confirmation/rejection/override; nominated/resolved/disputed lifecycle; customer and collections communication history; configurable four-stage reminder cadence; idempotent stage queueing; active-promise suppression; queue cancellation after arrears clear; broken-promise detection; owner-only escalations for disputed notices, broken promises, old arrears, and repeated reminder failures; open-exception deduplication.
- **UI:** Added Tolls & Fines New, Matched, Review Required, and Resolved queues; notice detail with evidence, matched assignment/customer, allocation controls and complete status history; Collections with overdue customers, reminder stage, promise state, next action, contact logging/history, and queue refresh; customer detail now includes communication and relevant toll/fine history. Navigation exposes both workspaces.
- **Tests/results:** `npm run lint` PASS; `npm run typecheck` PASS; `npm test` PASS (6 tests); `npm run build` PASS (20 app pages generated; new dynamic routes built); `npm run supabase:reset` PASS; `npm exec supabase -- test db` PASS (121 assertions across 5 files); `npm exec supabase -- db lint --local` PASS; `git diff --check` PASS. New coverage includes exact/no/ambiguous assignment-time matching, no silent ambiguous allocation, automated evidence preservation on manual override, unchanged assignment history, notice resolution lifecycle, reminder deduplication, cancellation after payment clears, promise creation/breakage, exception deduplication, unauthorized RLS denial, and inactive-staff denial.
- **Unresolved issues:** Reminder execution is request/manual-refresh driven and produces queue records only; production scheduling and SMS delivery are intentionally absent. Notice intake is manual metadata entry only; provider APIs and external credentials are absent. Starr365 nomination/write-back is intentionally absent. Reminder failure state is modeled for a future worker, but no delivery worker exists. Cadence defaults (1/7/14/21 days) are database-configurable and should be reviewed before staging. No production migration was run.
- **Security notes:** Authoritative mutations are authenticated server actions calling active-staff-checked security-definer RPCs; browser roles receive SELECT only and all new tables use deny-by-default RLS. Manual allocation requires an actor and reason, ambiguity cannot be confirmed, owner-risk exceptions are owner-only, and notice evidence excludes credentials/message bodies. Financial history and vehicle assignment history are not mutated by these workflows. Tests and examples are synthetic only.
- **Git status:** Changes remain uncommitted. The migration, pgTAP suite, server actions, and new pages are untracked; navigation, customer detail, and this progress file are modified. No V1 files were changed.
- **Recommended next task:** Review cadence and escalation thresholds with operations, exercise the queues end-to-end using synthetic local data, then design a separately reviewed staging-only scheduler/delivery worker with retry telemetry before any provider integration.
