# Codex progress

- **Current task:** Veera V2 staged bank/PayID reconciliation.
- **Status:** PASS — implementation and all requested local verification are complete. No commit, production migration, bank connection, production credential, or real-data operation was performed.
- **Files created:** `supabase/migrations/20260824100000_staged_bank_reconciliation.sql`; `supabase/tests/bank_reconciliation_test.sql`; `apps/web/src/lib/reconciliation-actions.ts`; `apps/web/src/components/synthetic-csv-import.tsx`; `apps/web/src/app/reconciliation/page.tsx`; `apps/web/src/app/reconciliation/[id]/page.tsx`; `apps/web/src/app/reconciliation/import/page.tsx`; `docs/bank-reconciliation.md`.
- **Files modified:** `apps/web/src/components/staff-nav.tsx`; `docs/CODEX_PROGRESS.md`.
- **Database changes:** Added durable import batches and imported transactions; immutable match runs/candidates, review actions, and payment postings; raw-value retention; checksum and external-ID idempotency; reversal linkage; indexes; deny-by-default RLS; active-staff read policies; controlled import/match/reconcile/auto-allocation/ignore/reversal RPCs; reconciliation audit actions; and seven deduplicated bank exception types.
- **Features implemented:** Synthetic CSV preview/import and rejected/duplicate batch results; deterministic evidence scoring with HIGH/MEDIUM/LOW/NO_MATCH/AMBIGUOUS states; HIGH-only auto-allocation; manual confirm/override/split with actor and reason; immutable payment-ledger reuse; partial/exact/multi-week/advance/excess behavior; duplicate-posting prevention; overdue refresh and queued reminder/delivery cancellation; ignore; compensating reversal; transaction detail evidence, obligations, results, and history; and reconciliation status queues.
- **Tests/results:** Added 28 focused pgTAP assertions. `npm run lint` PASS; `npm run typecheck` PASS; `npm test` PASS (17 tests); `npm run build` PASS (23 generated pages); `npm run supabase:reset` PASS; `npm exec supabase -- test db` PASS (191 assertions across 8 files); `npm exec supabase -- db lint --local` PASS; `git diff --check` PASS.
- **Unresolved issues:** No task-blocking issues. Real bank feeds, production PayID APIs, Renta, Starr365, arbitrary Excel, production migration, and real customer data remain intentionally out of scope. The initial UI supports at most three agreement splits per receipt; the database RPC accepts an arbitrary non-empty allocation array.
- **Security notes:** No banking secrets or provider code were added. `SYNTHETIC_CSV` is the only source. Upload parsing and hashing occur server-side; browser roles have read-only table grants; all authoritative mutations repeat active-staff checks. Audit metadata excludes payer names, raw bank descriptions/references, phone numbers, and credentials. Tests use synthetic identities and finances only.
- **Git status:** Changes remain uncommitted as requested: one tracked source file and this progress file modified, with the reconciliation migration, test, docs, server action, component, and three pages untracked/new.
- **Recommended next task:** Review this staged boundary and matching thresholds with finance/operations using synthetic fixtures, then add a provider-neutral bank-feed adapter design and threat model without connecting any production account.
- **Stop point:** Stop after this staged bank reconciliation task.
