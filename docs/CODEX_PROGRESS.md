# Codex progress

- **Current task:** Veera V2 customer approval, customer/pickup/return readiness, vehicle compliance, and maintenance/service tracking.
- **Status:** PASS — implementation complete and all requested local verification passed. No commit, production migration, external integration, real files, or real customer data was created.
- **Files created:** `supabase/migrations/20260824050000_customer_readiness_compliance_maintenance.sql`; `supabase/tests/readiness_maintenance_test.sql`; `apps/web/src/lib/operations-actions.ts`; operational pages under `apps/web/src/app/operations/` for customers, compliance, pickups, returns, maintenance due, and service-job detail.
- **Files modified:** `apps/web/src/components/staff-nav.tsx`; `apps/web/src/lib/owner.ts`; the three existing pgTAP suites to provision synthetic readiness prerequisites; this progress file.
- **Database changes:** Added durable customer approvals, customer-document metadata, vehicle compliance, immutable odometer readings, maintenance plans/jobs, pickup checklists, and return checklists. Added a default PENDING approval initializer, 10,000 km plan default and generated next-service threshold, current readiness/maintenance views, off-road duration tracking, deny-by-default RLS/read-only staff policies, partial uniqueness for open service jobs, and server-only atomic RPC workflows. Expanded audit actions and owner exception types. Replaced the legacy assignment RPC so it cannot bypass customer or vehicle readiness.
- **Features implemented:** Admin-only approval decisions with preserved audit history; staff document/compliance preparation; expiry-aware readiness; pickup activation gated by approval, both verified customer documents, ready/active agreement, assigned agreement vehicle, valid rego/RWC, vehicle availability, and nondecreasing pickup odometer; explicit return condition/open-issue/disposition state with release/workshop/off-road routing; immutable custody history; odometer history; service job open/complete workflows; reliable due/overdue derivation; automatic next-service advancement; workshop/off-road integration; owner exception synthesis for pending approvals, blocked pickup/return, licence/rego/RWC expiry, service due/overdue, and prolonged off-road vehicles; unresolved exception deduplication.
- **UI:** Added functional approval/readiness queue, document metadata controls, pickup checklist, return checklist, compliance register, service-due list, and service-job detail. Owner dashboard refresh now includes the new exception sources. Navigation links expose all new operational views.
- **Tests/results:** `npm run lint` PASS; `npm run typecheck` PASS; `npm test` PASS (6 tests); `npm run build` PASS (all 18 routes built); `npm run supabase:reset` PASS; `npm exec supabase -- test db` PASS (95 assertions across 4 files); `npm exec supabase -- db lint --local` PASS; `git diff --check` PASS. Coverage includes every requested readiness, expiry, odometer, service threshold, history, deduplication, RLS, and approval-authority case.
- **Unresolved issues:** File upload/storage is intentionally metadata-only. Existing legacy `/assign` and `/return` pages remain present for compatibility, but navigation directs staff to authoritative checklist pages. Exception refresh is request-driven from the owner dashboard; production scheduling was not configured. The expiring-soon horizon is currently 30 days and service overdue is 1,000 km beyond due; these are explicit defaults that can be made configurable later.
- **Security notes:** Browser mutations use authenticated server actions calling security-definer RPCs; operational tables expose SELECT only under active-staff RLS and have no browser write policies. Approval requires active ADMIN authority. Every authoritative workflow rechecks staff/admin status server-side. Odometer and state transitions execute under row locks/transactions. No service-role key, production credential, PII, or customer file was added; all tests use synthetic identities/data.
- **Git status:** Changes remain uncommitted. New migration, test, server action, and operational pages are untracked; navigation, owner refresh, legacy tests, and this file are modified. No V1 files were changed.
- **Recommended next task:** Exercise the new flows end-to-end in the local UI with synthetic customers/vehicles, then add private Supabase Storage and reviewed file-access policies when real document upload enters scope.
