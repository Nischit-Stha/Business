# Codex progress

- **Current task:** Veera V2 staff authentication and controlled customer/vehicle management
- **Status:** PASS
- **Files created:** `supabase/migrations/20260824020000_staff_customer_vehicle_management.sql`; `apps/web/src/lib/auth.ts`, `auth-actions.ts`, `management-actions.ts`, `validation.ts`, and `validation.test.ts`; `apps/web/src/components/staff-nav.tsx` and `management-forms.tsx`; Login, Access Denied, Assign, Return, Swap, Customer New/Detail, and Vehicle New/Detail pages.
- **Files modified:** `apps/web/src/app/page.tsx`, `styles.css`, Fleet/Customers/Assignments pages, `apps/web/src/lib/fleet.ts`, `fleet-actions.ts`, `supabase/tests/business_core_test.sql`, and `docs/admin-workflows.md`.
- **Database changes:** Added server-controlled staff `role` (`ADMIN`/`STAFF`) and `status` (`ACTIVE`/`DISABLED`); updated active-staff authorization; expanded audit actions; added locked customer create/update/status, vehicle create/update, and admin staff-access RPCs. Operational tables retain read-only authenticated grants and deny-by-default RLS. Vehicle authoring rejects assignment-state transitions, duplicate registration remains case-insensitively constrained, and odometers cannot move backwards.
- **Features implemented:** Supabase password sign-in and sign-out with no signup; server-side active staff-profile checks and explicit non-staff denial; documented first-admin and ongoing admin-controlled provisioning; customer create/view/edit/status workflows; vehicle create/view/edit/non-assignment-status workflows; simple operational Fleet, Customers, Assign, Return, and Swap pages. Assignment changes continue through the existing atomic custody RPCs.
- **Tests/results:** `npm run lint` PASS; `npm run typecheck` PASS; `npm test` PASS (6 tests); `npm run build` PASS; `npm run supabase:reset` PASS; `npm exec supabase -- test db` PASS (28 tests); `npm exec supabase -- db lint --local` PASS; `git diff --check` PASS. Database coverage includes non-staff and disabled-staff denial, customer create/validation/update/status/audit, vehicle create/duplicate registration/update/audit, direct-write denial, and manual `ASSIGNED` bypass denial.
- **Unresolved issues:** The first administrator still requires the documented controlled Auth + SQL bootstrap; there is intentionally no public or in-app staff registration. There is no licence image upload. Agreements, payments, maintenance, tolls/fines, Renta, Starr365, PayID, SMS, production migration, and production provisioning remain out of scope. Assignment form errors currently surface through the server error boundary rather than inline form state.
- **Security notes:** Authorization is derived only from the authenticated user ID and `staff_profiles`, never `user_metadata`. Every operational page/data loader checks the session and active profile server-side. Authenticated browser sessions have no table mutation grants; all writes use narrowly scoped, staff-checking security-definer functions. No service-role key, production credential, real PII, or customer document was added. Audit metadata omits customer contact, address, licence number, and vehicle VIN.
- **Git status:** Existing and task changes remain uncommitted. Modified: operational pages/styles, fleet server modules, admin workflow docs, and database tests. New/untracked: authentication/management modules and components, new operational pages, and the forward-only management migration. No commit created.
- **Recommended next task:** Review the authentication and CRUD workflows in local UI, then add a secured administrator-only staff management page or proceed to the separately reviewed assignment lifecycle enhancements. Do not begin production migration or financial integrations yet.
