# Codex progress

- **Current task:** Veera V2 secure private document storage and staff document upload workflows.
- **Status:** PASS — implementation and security hardening are complete; every requested local verification command passes. No commit or production operation was performed.
- **Files created:** `supabase/migrations/20260824080000_document_storage_server_control.sql`; `apps/web/src/lib/supabase/privileged-storage.ts`; `apps/web/src/lib/document-validation.ts`; `apps/web/src/lib/document-validation.test.ts`.
- **Files modified:** `.env.example`; `apps/web/src/lib/document-actions.ts`; `apps/web/src/lib/env.ts`; `apps/web/src/lib/env.test.ts`; `supabase/tests/document_storage_test.sql`; `docs/document-storage-security.md`; `docs/CODEX_PROGRESS.md`.
- **Existing task files retained:** `supabase/migrations/20260824070000_private_document_storage.sql`; `apps/web/src/components/document-workflow.tsx`; customer and fleet detail-page integrations. No V1 file was changed.
- **Database changes:** Added a forward-only hardening migration that removes all authenticated `storage.objects` read/write policies, replaces upload registration with a `service_role`-only function, independently revalidates the supplied active `ADMIN`/`STAFF` actor, preserves actor attribution/version supersession/audit writes atomically, and requires expiry for licences, registration, and RWC.
- **Storage changes:** The two 10 MiB PDF/JPEG/PNG buckets remain private. Browser/session roles can no longer upload, list, read, update, or delete objects. The authenticated server action performs narrowly scoped object operations through a server-only privileged client, uses generated non-reused paths and `upsert: false`, and compensates for metadata failure by removing the newly uploaded orphan.
- **Features implemented:** Staff workflows cover driver licence, proof of address, registration, and RWC upload; byte-signature detection; filename normalization; SHA-256; internal UUID paths; immutable version history; verify/reject; expiry projection; secure 60-second viewing; and upload/view/verify/reject/supersede audit events. View authorization and audit recording happen before signed URL generation.
- **UI:** Customer and vehicle details retain functional controls for upload, status/expiry, verify/reject, secure current-document viewing, and version history.
- **Tests/results:** `npm run lint` PASS; `npm run typecheck` PASS; `npm test` PASS (13 tests); `npm run build` PASS (20 generated static pages plus dynamic routes); `npm run supabase:reset` PASS; `npm exec supabase -- test db` PASS (140 assertions across 6 files); `npm exec supabase -- db lint --local` PASS; `git diff --check` PASS. Tests cover content-signature detection, unsupported content, size limits, secure path generation, filename sanitization, authorized upload metadata, browser registration denial, inactive/unauthorized actors, replacements, verification, signed-view authorization, audit events, expiry readiness/pickup effects, and private buckets with no browser object policies.
- **Unresolved issues:** Malware scanning/quarantine and an approved retention purge remain future work. Local Storage schema and authorization behavior were verified, but a synthetic binary browser-to-Storage end-to-end test was not automated. A process crash between successful object upload and the compensating cleanup could still leave an unreferenced private object; production monitoring/reconciliation should detect this rare case.
- **Security notes:** `SUPABASE_SERVICE_ROLE_KEY` is server-only and must be supplied by a local/staging secret store, never prefixed `NEXT_PUBLIC`, committed, logged, or reused from production. Signed links expire after 60 seconds; their values and document contents are excluded from audit context. Original filenames never enter object paths. No real customer data or production credentials were used.
- **Git status:** Uncommitted changes are limited to the files listed above; four new implementation files are untracked. No commit was created.
- **Recommended next task:** In a separately controlled staging environment, add quarantined malware scanning, retention/legal-hold rules, orphan-object reconciliation, and a synthetic end-to-end upload/view test before migrating any real documents.
