import { PortalNav } from '@/components/portal-nav';import { requireCustomer } from '@/lib/auth';import { submitPortalIssue } from '@/lib/portal-actions';
const labels:Record<string,string>={REPORTED:'Reported',BEING_REVIEWED:'Being reviewed',IN_PROGRESS:'In progress',WAITING:'Waiting',RESOLVED:'Resolved'};
export default async function PortalIssues({searchParams}:{searchParams:Promise<{error?:string;created?:string}>}){const {error,created}=await searchParams;const {supabase}=await requireCustomer();const {data}=await supabase.from('portal_issues').select('*').order('created_at',{ascending:false});return <main className="portal-shell"><PortalNav/><h1>Vehicle issues</h1>{error&&<p className="error">{error}</p>}{created&&<p className="success">Your issue was reported to Veera.</p>}<section className="portal-card portal-action"><h2>Report a vehicle issue</h2><p>If there is immediate danger, move to a safe location and contact emergency services first.</p><form action={submitPortalIssue} className="form-card"><label>Category<select name="category">{['WARNING_LIGHT','BREAKDOWN','TYRE','BATTERY','DAMAGE','PARKING','ACCIDENT','SERVICE','CLEANING','CUSTOMER_COMPLAINT','OTHER'].map(x=><option key={x}>{x.replaceAll('_',' ')}</option>)}</select></label><label>How serious does it seem?<select name="severity"><option>LOW</option><option>MEDIUM</option><option>HIGH</option><option>CRITICAL</option></select></label><label>Short description<textarea name="description" required maxLength={500}/></label><label>Optional note<textarea name="note" maxLength={500}/></label><button>Report issue</button></form></section><h2>Your reported issues</h2><div className="portal-list">{data?.length?data.map(i=><article key={i.id}><strong>{i.category.replaceAll('_',' ')}</strong><b>{labels[i.customer_status]??i.customer_status}</b><span>Reported {new Date(i.created_at).toLocaleDateString('en-AU')}</span>{i.customer_description&&<p>{i.customer_description}</p>}</article>):<p className="portal-card">You have no reported issues.</p>}</div></main>}
