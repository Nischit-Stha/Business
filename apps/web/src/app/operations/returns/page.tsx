import {StaffNav} from '@/components/staff-nav';import {requireStaff} from '@/lib/auth';import {createReturn,completeReturn} from '@/lib/operations-actions';
export default async function Page({searchParams}:{searchParams:Promise<{error?:string}>}){const {supabase}=await requireStaff();const {error}=await searchParams;const [{data:assignments},{data:items}]=await Promise.all([supabase.from('vehicle_assignments').select('id,customers(full_name),vehicles(registration)').eq('assignment_status','ACTIVE'),supabase.from('return_checklists').select('*,vehicle_assignments(customers(full_name),vehicles(registration))').neq('status','COMPLETED')]);return <main><StaffNav/><p className="eyebrow">Return control</p><h1>Return checklists</h1>{error&&<p className="error">{error}</p>}<form action={createReturn} className="form-card"><label>Active assignment<select name="assignmentId">{assignments?.map(a=><option key={a.id} value={a.id}>{a.vehicles?.[0]?.registration} — {a.customers?.[0]?.full_name}</option>)}</select></label><button>Prepare return</button></form>{items?.map(i=><form key={i.id} action={completeReturn} className="form-card"><input type="hidden" name="checklistId" value={i.id}/><strong>{i.vehicle_assignments?.[0]?.vehicles?.[0]?.registration} — {i.vehicle_assignments?.[0]?.customers?.[0]?.full_name}</strong><label>Return km<input name="odometer" type="number" min="0" required/></label><label>Condition<select name="condition"><option>GOOD</option><option>DAMAGE_NOTED</option><option>UNSAFE</option></select></label><label><input type="checkbox" name="openIssue"/> Open issue</label><label>Route<select name="disposition"><option>RELEASE</option><option>WORKSHOP</option><option>OFF_ROAD</option></select></label><button>Complete return</button></form>)}</main>}
