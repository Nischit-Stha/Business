import { redirect } from 'next/navigation';
import { updatePassword } from '@/lib/account-actions';
import { createSupabaseServerClient } from '@/lib/supabase/server';
export default async function ResetPassword({searchParams}:{searchParams:Promise<{error?:string}>}){const supabase=await createSupabaseServerClient();if(!(await supabase.auth.getUser()).data.user)redirect('/login');const {error}=await searchParams;return <main className="auth-shell"><p className="eyebrow">Secure account</p><h1>Choose a password</h1><p>Use at least 12 characters. Other sessions are revoked after the change.</p>{error&&<p className="error" role="alert">{error}</p>}<form action={updatePassword} className="form-card"><label>New password<input name="password" type="password" autoComplete="new-password" minLength={12} required/></label><label>Confirm password<input name="confirm" type="password" autoComplete="new-password" minLength={12} required/></label><button>Save password</button></form></main>}
