import { VehicleForm } from '@/components/management-forms'; import { StaffNav } from '@/components/staff-nav'; import { requireStaff } from '@/lib/auth';
export default async function NewVehiclePage({ searchParams }: { searchParams: Promise<{ error?: string }> }) { await requireStaff(); const { error } = await searchParams; return <main><StaffNav/><p className="eyebrow">Fleet</p><h1>New vehicle</h1>{error && <p className="error">{error}</p>}<VehicleForm/></main>; }
