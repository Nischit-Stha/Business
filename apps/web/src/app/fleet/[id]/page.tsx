import { notFound } from 'next/navigation'; import { VehicleForm } from '@/components/management-forms'; import { StaffNav } from '@/components/staff-nav'; import { requireStaff } from '@/lib/auth';
export default async function VehicleDetail({ params, searchParams }: { params: Promise<{ id: string }>; searchParams: Promise<{ error?: string }> }) { const { id } = await params; const { supabase } = await requireStaff(); const { data } = await supabase.from('vehicles').select('*').eq('id', id).maybeSingle(); if (!data) notFound(); const { error } = await searchParams; return <main><StaffNav/><p className="eyebrow">Vehicle detail</p><h1>{data.registration}</h1>{error && <p className="error">{error}</p>}<p>Status: <strong>{data.operational_status.replaceAll('_',' ')}</strong></p><VehicleForm vehicle={data}/></main>; }
